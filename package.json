{
  "name": "@bheath-atx/miser",
  "version": "0.1.0",
  "description": "Local failover routing and billing observability for Claude Code and the TermDeck stack",
  "main": "src/index.js",
  "bin": {
    "miser-watchd": "bin/miser-watchd.js",
    "make-lane-prompt": "bin/make-lane-prompt.js",
    "spawn-lane.sh": "bin/spawn-lane.sh",
    "boot-inject.sh": "bin/boot-inject.sh",
    "td-inject.sh": "bin/td-inject.sh"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --require ./test/live-file-guard.js --test test/*.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT",
  "keywords": ["termdeck", "claude", "proxy", "failover", "observability", "ollama"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bheath-atx/miser.git"
  }
}
